<div class="container mt-4">
  <div class="text-center my-4">
    <h1>Candidatos</h1>
  </div>
  <% if user_signed_in? %>
    <div class="text-right">
      <%= link_to "Minha página", user_root_path, class: "btn btn-gradient" %>
    </div>
  <% end %>

  <div class="container">
    <div class="row">
      <% @candidatos.first(50).each do |candidato| %>
        <div class="col-sm-6 col-lg-3 mb-4 d-flex align-items-stretch justify-content-center">
          <%= render "shared/card_candidato", candidato: candidato %>
        </div>
      <% end %>
    </div>
    <%= paginate @candidatos %>
  </div>

  <div class="container text-right">
    <hr>
    <a href="#">Voltar ao topo</a>
    <br>
    <br>
</div>
