<%# document.querySelector('.log-modal').innerHTML = "<%= escape_javascript(render 'shared/login_modal')%>

const errorMessage = document.querySelector('.errors')
errorMessage.innerHTML = "Nome/Email/Password inválidos. \<br>Por favor, reveja as informações inseridas:"
